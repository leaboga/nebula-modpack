<UserControl x:Class="NebulaLauncher.Modules.ModManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Modern Toggle Switch Style -->
        <Style x:Key="ModernToggle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Background" Width="44" Height="22" CornerRadius="11" Background="#1A1528" BorderBrush="#2E2648" BorderThickness="1" Cursor="Hand">
                            <Ellipse x:Name="Dot" Width="14" Height="14" Fill="#4A4266" HorizontalAlignment="Left" Margin="3,0,0,0">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform X="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Background" Property="Background" Value="#22C55E"/>
                                <Setter TargetName="Background" Property="BorderBrush" Value="#22C55E"/>
                                <Setter TargetName="Dot" Property="Fill" Value="White"/>
                                <Setter TargetName="Dot" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Dot" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,24">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Gestión Local" Foreground="White" FontSize="26" FontWeight="Black"/>
                <TextBlock Text="ADMINISTRAR MODS INSTALADOS" Foreground="#6B5E8A" FontSize="10" FontWeight="Bold" Margin="2,2,0,0" CharacterSpacing="100"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="📂 Abrir Carpeta" Style="{StaticResource SecondaryButton}" Height="34" Width="130" Click="BtnOpenFolder_Click" Margin="0,0,10,0"/>
                <Button Content="🔄 Refrescar" Style="{StaticResource SecondaryButton}" Height="34" Width="100" Click="BtnRefresh_Click"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="#0D0B1899" CornerRadius="20" BorderBrush="#1E1A2E" BorderThickness="1" Padding="4">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="12">
                    <ItemsControl x:Name="ModItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#12101E" CornerRadius="14" Margin="0,0,0,8" Padding="16,12" BorderBrush="#1E1A2E" BorderThickness="1">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="#2E2648"/>
                                                    <Setter Property="Background" Value="#161421"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Width="42" Height="42" CornerRadius="10" Background="#1F1B2E" VerticalAlignment="Center">
                                            <TextBlock Text="📦" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>

                                        <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding FileName}" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding FileSize}" Foreground="#4A4266" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Orientation="Horizontal">
                                            <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontSize="10" FontWeight="Black" Margin="0,0,16,0" VerticalAlignment="Center" Opacity="0.8"/>
                                            <CheckBox IsChecked="{Binding IsEnabled}" Style="{StaticResource ModernToggle}" Click="ModToggle_Click" Tag="{Binding FileName}" VerticalAlignment="Center"/>
                                            
                                            <Border Width="1" Height="20" Background="#1E1A2E" Margin="16,0"/>
                                            
                                            <Button Content="🗑️" Background="Transparent" BorderThickness="0" Foreground="#4A4266" FontSize="14" Cursor="Hand" Click="DeleteMod_Click" Tag="{Binding FileName}" ToolTip="Eliminar mod permanentemente">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Foreground" Value="#EF4444"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Empty State -->
                <StackPanel x:Name="NoModsAlert" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
                    <TextBlock Text="👽" FontSize="48" HorizontalAlignment="Center" Opacity="0.5"/>
                    <TextBlock Text="No se encontraron mods" Foreground="#4A4266" FontSize="16" FontWeight="Bold" Margin="0,12,0,4"/>
                    <TextBlock Text="Usa el Mod Hub para descargar contenido" Foreground="#3D3560" FontSize="12" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

