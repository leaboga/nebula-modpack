<UserControl x:Class="NebulaLauncher.Modules.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#0A0714">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Padding="20,15" Background="#1A1528" BorderBrush="#2E2648" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="TERMINAL DE SISTEMA" Foreground="#7B6FxA0" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                <Border Width="8" Height="8" Background="#FF3B30" CornerRadius="4" Margin="15,0,5,0"/>
                <TextBlock Text="LIVE FEED" Foreground="#FF3B30" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20,12,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="FilterBox"
                     Grid.Column="0"
                     Height="34"
                     Margin="0,0,12,0"
                     VerticalContentAlignment="Center"
                     Style="{DynamicResource SearchTextBox}"
                     Tag="Filtrar logs..."
                     TextChanged="FilterBox_TextChanged"/>
            <Button Grid.Column="1" Content="Exportar" Style="{DynamicResource SecondaryButton}" Height="34" Margin="0,0,10,0" Click="Export_Click"/>
            <Button Grid.Column="2" Content="Abrir archivo" Style="{DynamicResource SecondaryButton}" Height="34" Margin="0,0,10,0" Click="OpenFile_Click"/>
            <Button Grid.Column="3" Content="Limpiar" Style="{DynamicResource SecondaryButton}" Height="34" Click="Clear_Click"/>
        </Grid>

        <ScrollViewer x:Name="LogScroll" Grid.Row="2" Padding="20">
            <TextBox x:Name="LogOutput" 
                     Background="Transparent" BorderThickness="0" 
                     Foreground="#A0AEC0" FontSize="12" FontFamily="Consolas"
                     IsReadOnly="True" TextWrapping="Wrap" 
                     AcceptsReturn="True"/>
        </ScrollViewer>
    </Grid>
</UserControl>
