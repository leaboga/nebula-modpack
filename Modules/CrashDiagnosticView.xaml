<UserControl x:Class="KrakenLauncher.Modules.CrashDiagnosticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="550" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="DIAGNÓSTICO Y SOPORTE" Foreground="White" FontSize="26" FontWeight="Bold"/>
            <TextBlock Text="Herramientas avanzadas para resolución de conflictos y gestión de permisos." Foreground="#6D8A99" FontSize="12"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- PERMISSIONS CARD -->
                <Border Background="#0D151D" CornerRadius="16" Padding="24" Margin="0,0,0,16" BorderBrush="#1E2C38" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="PRIVILEGIOS DE SISTEMA" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Black" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <TextBlock Text="ESTADO ACTUAL: " Foreground="White" FontSize="14" FontWeight="Bold"/>
                                <TextBlock x:Name="AdminStatusLabel" Text="USUARIO ESTÁNDAR" Foreground="#EF4444" FontSize="14" FontWeight="Bold"/>
                            </StackPanel>
                            <TextBlock Text="Algunas operaciones de escritura o actualización pueden requerir permisos elevados." Foreground="#6D8A99" FontSize="11" TextWrapping="Wrap" MaxWidth="450"/>
                        </StackPanel>
                        <Button x:Name="ElevateBtn" Grid.Column="1" Content="🛡️ Ejecutar como Administrador" Style="{DynamicResource PlayButton}" Height="40" Padding="20,0" VerticalAlignment="Center" Click="Elevate_Click"/>
                    </Grid>
                </Border>

                <!-- CONFIGS & PATHS CARD -->
                <Border Background="#0A0E14" CornerRadius="16" Padding="24" Margin="0,0,0,16" BorderBrush="#1E2C38" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="RUTAS Y CONFIGURACIONES" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Black" Margin="0,0,0,16"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="CARPETA APP" Foreground="#6D8A99" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock x:Name="AppPathLabel" Grid.Column="1" Text="C:\Users\...\KrakenLauncher" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="10,0"/>
                            <Button Grid.Column="2" Content="📂 Abrir" Style="{DynamicResource SecondaryButton}" Height="32" Padding="12,0" Click="OpenAppFolder_Click"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="INSTANCIAS" Foreground="#6D8A99" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock x:Name="InstancesPathLabel" Grid.Column="1" Text="C:\Users\...\instances" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="10,0"/>
                            <Button Grid.Column="2" Content="📂 Abrir" Style="{DynamicResource SecondaryButton}" Height="32" Padding="12,0" Click="OpenInstancesFolder_Click"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="EJECUTABLE" Foreground="#6D8A99" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock x:Name="ExePathLabel" Grid.Column="1" Text="KrakenLauncher.exe" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="10,0"/>
                            <Button Grid.Column="2" Content="📋 Copiar" Style="{DynamicResource SecondaryButton}" Height="32" Padding="12,0" Click="CopyExePath_Click"/>
                        </Grid>

                        <Separator Background="#1E2C38" Margin="0,10"/>

                        <UniformGrid Columns="3" Margin="0,10,0,0">
                            <Button Content="📄 Ver launcher.log" Style="{DynamicResource SecondaryButton}" Height="38" Margin="0,0,8,0" Click="OpenLogs_Click"/>
                            <Button Content="⚙️ Ver session.json" Style="{DynamicResource SecondaryButton}" Height="38" Margin="4,0,4,0" Click="OpenSession_Click"/>
                            <Button Content="🕒 Ver updater.log" Style="{DynamicResource SecondaryButton}" Height="38" Margin="8,0,0,0" Click="OpenUpdaterLog_Click"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- CRASH REPORTS CARD -->
                <Border Background="#0D151D" CornerRadius="16" Padding="24" BorderBrush="#1E2C38" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="ANÁLISIS DE ERRORES" Foreground="{DynamicResource AccentBrush}" FontSize="10" FontWeight="Black" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock x:Name="StatusLabel" Text="Sistema Saludable" Foreground="#10B981" FontSize="18" FontWeight="Bold"/>
                                <TextBlock x:Name="DetailLabel" Text="No se detectaron fallos críticos recientemente." Foreground="#6D8A99" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="🔍 Analizar" Style="{DynamicResource SecondaryButton}" Height="40" Padding="20,0" Click="Analyze_Click"/>
                                <Button Content="📤 Reportar" Style="{DynamicResource SecondaryButton}" Height="40" Padding="20,0" Margin="10,0,0,0" Click="ExportReport_Click"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
