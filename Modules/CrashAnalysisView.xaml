<UserControl x:Class="KrakenLauncher.Modules.CrashAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <Grid Margin="40">
        <Border Background="#0D151D" CornerRadius="24" BorderBrush="#EF4444" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#EF4444" Opacity="0.2" BlurRadius="50" ShadowDepth="0"/>
            </Border.Effect>
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                    <Border Width="50" Height="50" CornerRadius="12" Background="#20EF4444">
                        <TextBlock Text="💥" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Anomalía Detectada" Foreground="White" FontSize="24" FontWeight="Bold"/>
                        <TextBlock x:Name="CrashDateLabel" Text="Minecraft cerró de forma inesperada" Foreground="#6D8A99" FontSize="13"/>
                    </StackPanel>
                </StackPanel>

                <!-- Error Summary Card -->
                <Border Grid.Row="1" Background="#0A0E14" CornerRadius="16" Padding="20" Margin="0,0,0,20" BorderBrush="#1E2C38" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="diagnóstico KRAKEN:" Foreground="#EF4444" FontSize="10" FontWeight="Black" Margin="0,0,0,8"/>
                        <TextBlock x:Name="ErrorTitleLabel" Text="Cargando análisis..." Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock x:Name="ErrorDescriptionLabel" Text="Estamos analizando los logs para encontrar la causa raíz..." Foreground="#6D8A99" FontSize="13" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Log Preview -->
                <Border Grid.Row="2" Background="#05080D" CornerRadius="12" Padding="15" BorderBrush="#0D151D" BorderThickness="1">
                    <ScrollViewer>
                        <TextBlock x:Name="LogPreviewText" Foreground="#344A5B" FontSize="10" FontFamily="Consolas" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>

                <!-- Actions -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="📁 Abrir Carpeta de Logs" Style="{StaticResource SecondaryButton}" Width="180" Height="40" Margin="0,0,12,0" Click="OpenLogs_Click"/>
                    <Button x:Name="FixButton" Content="🛠 Aplicar Solución Sugerida" Style="{StaticResource PlayButton}" Width="220" Height="40" Click="Fix_Click" Background="#EF4444"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
