<UserControl x:Class="KrakenLauncher.Modules.HubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Dynamic Tab Bar -->
        <Border Grid.Row="0" Margin="32,0,32,8" Background="#66081218" CornerRadius="16" BorderBrush="#223A44" BorderThickness="1" Padding="8,6">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled">
                <StackPanel x:Name="TabContainer" Orientation="Horizontal"/>
            </ScrollViewer>
        </Border>

        <!-- Content Area -->
        <ContentControl x:Name="ActiveModuleContainer" Grid.Row="1"/>
    </Grid>
</UserControl>
